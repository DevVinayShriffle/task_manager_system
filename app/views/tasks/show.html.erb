<h1>Task Details</h1>
<p>Title: <%= @task.title %></p>
<p>Descryption: <%= @task.descryption %></p>
<p>Status: <%= @task.status %></p>

<%= link_to 'Edit task', edit_task_path(@user) %> |
<%= link_to 'Back', tasks_path %> |

<%= link_to 'Delete task', task_path(@task), data: { turbo_method: :delete, turbo_confirm: 'Are you sure you want to delete this task?' } %>
